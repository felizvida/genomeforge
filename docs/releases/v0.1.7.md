# Genome Forge v0.1.7

Release date: 2026-04-02

## Highlights

- Refined the tutorial into a publication-grade edition with true front matter, a case-level table of contents, and chapter-style cluster openers.
- Tightened the final copyfit pass so the book-length tutorial reads more cleanly in both HTML and PDF.
- Improved the PDF export so it now carries author/subject metadata and reports as a tagged PDF.

## Validation

- `python3 -m py_compile docs/tutorial/generate_tutorial.py docs/build_tutorial_pdf.py`
- `python3 docs/tutorial/generate_tutorial.py`
- `python3 docs/validate_docs.py`
- `./.venv-docs/bin/python docs/build_tutorial_pdf.py`

## Notes

- The local PDF build still emits non-fatal `fontconfig` cache warnings on this machine, but the build succeeds and writes the final PDF artifact.
